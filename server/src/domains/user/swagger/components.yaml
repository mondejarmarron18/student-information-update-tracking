components:
  schemas:
    user:
      type: object
      properties:
        roleId:
          type: string
          description: Role ID
        email:
          type: string
          format: email
          description: Email provided by the school
