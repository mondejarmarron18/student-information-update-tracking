components:
  schemas:
    role:
      type: object
      properties:
        name:
          type: string
          description: Role name
          example: student
        description:
          type: string
